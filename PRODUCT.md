# Product

## Register

数字人 Agent 控制台

## Users

面向数字人 Agent 的产品、算法、运营和平台工程团队。用户通常在桌面浏览器中工作，需要快速确认数字人运行状态、检查 Agent/RAG/MCP 链路、定位异常并比较评测结果，而不是阅读底层日志或配置细节。

## Product Purpose

这是通用数字人 Agent 的控制台与运行工作台。它把数字人 Provider、LangGraph Agent、RAG、MCP、会话状态和 FutureAGI 可观测性放在同一个可信入口中，让使用者可以建立会话、实时交流、检索知识、评测质量、回放 Trace，并在不暴露敏感凭证的前提下维护运行配置。

成功标准是：用户能在首次进入后迅速建立一个可用会话；在实时对话中能看懂当前状态并随时改口；在 RAG、评测和审计页面能通过人类可读的指标和图表判断问题落点；新增 Provider 或数据源时不需要重写控制台。

## Brand Personality

克制、可靠、清晰。界面应让复杂的实时系统感觉有秩序、有反馈、可追踪；语气直接而友好，错误说明行动路径，不把内部对象名、原始 payload 或密钥暴露给用户。

## Anti-references

避免深色赛博风、发光渐变、装饰性网格、巨型标题、堆叠式营销卡片、纯日志终端和把截图直接当成界面背景。避免为了展示技术而让用户先理解事件 ID、内部枚举或厂商原始字段。

## Design Principles

1. 状态先于装饰：连接、录音、生成、改口和异常都要在第一视线内给出明确反馈。
2. 人类可读的可观测性：把链路事件转换为阶段、耗时、结果和下一步动作。
3. 高密度但不拥挤：桌面端支持扫描和比较，移动端按任务顺序折叠，不牺牲可操作性。
4. 运行时可替换：Provider、模型、RAG 和工具展示遵循统一组件契约，新增实现不改变用户心智模型。
5. 谨慎的实时体验：首响、播放、心跳和中断反馈优先，任何降级都要诚实说明。

## Accessibility & Inclusion

目标为 WCAG 2.1 AA。所有图标按钮提供可见焦点、aria-label 和悬停说明；状态不能只依赖颜色；支持键盘导航、中文输入法、窄屏布局和 prefers-reduced-motion。正文与控件文本保持足够对比度，动态状态使用 aria-live 但避免高频噪声。
