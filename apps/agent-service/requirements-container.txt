fastapi>=0.115,<1.0
httpx>=0.27,<1.0
langgraph>=0.2
mcp>=2.0,<3.0
pydantic>=2.7,<3.0
sse-starlette>=3.0,<4.0
uvicorn[standard]>=0.30,<1.0
aio-pika>=9.4,<10.0
redis>=5.0,<7.0
playwright>=1.50,<2.0
docling>=2.0,<3.0
# Docling's RapidOCR engine only ships the ONNX weights; the runtime itself is
# an optional extra. Without it every PDF/image ingest fails at conversion time
# with ImportError, while health checks still look green because the converter
# is built lazily. Pin the CPU build so no CUDA wheels leak into the image.
onnxruntime>=1.18,<2.0
faiss-cpu>=1.9,<2.0
sentence-transformers>=3.0,<6.0
fi-instrumentation-otel>=1.1,<2.0; python_version < "3.14"
