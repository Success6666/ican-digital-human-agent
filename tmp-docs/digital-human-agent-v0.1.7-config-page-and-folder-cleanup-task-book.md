# v0.1.7 配置页与目录清理任务书

## 原始问题

1. 仓库正式目录为 `ican数字人`，旧的拼写目录 `icna数字人` 仍存在，需要删除。
2. 后台配置页当前仅展示配置入口，缺少可编辑弹窗、保存接口和保存后的读取回显，导致配置无法真正生效。

## 修复范围

- 删除旧目录 `E:\icna数字人`，不改动正式仓库目录。
- 梳理前端后台配置模块与后端配置存储边界。
- 为数字人、LLM、Embedding、系统配置提供统一的可编辑弹窗。
- 保存后重新读取并展示人类可读的配置摘要；敏感字段不得明文回显。
- 增加前后端测试与 Docker 部署验证。

## 验收标准

- [x] 旧目录内容已清空并进入删除流程；正式目录 `E:\ican数字人` 正常可用。
- [x] 后台配置页每类配置均可打开弹窗、编辑、保存、取消。
- [x] 刷新页面后已保存配置仍能正确回显。
- [x] 配置写入失败时有明确错误提示，不覆盖旧配置。
- [x] 密钥字段使用掩码展示，接口不向普通页面泄露完整密钥。
- [x] 前端构建、后端测试和配置页浏览器回归通过。

## 验证记录

- 管理员导航：首页、RAG、评测、审计、后台配置。
- 普通用户导航：首页、RAG；不可见评测、审计、后台配置。
- 管理员打开魔珐星云弹窗可看到 App ID、App Secret、网关等配置项。
- 管理员配置保存接口返回 `200`，服务端返回掩码后的凭证状态。
- Agent 配置测试：4 passed。
- Agent 全量测试：159 passed, 1 skipped。
- Web 构建：通过。
- Docker：Web、Agent、Auth、MCP 均 healthy。

## 状态

目录删除待释放句柄后完成。

