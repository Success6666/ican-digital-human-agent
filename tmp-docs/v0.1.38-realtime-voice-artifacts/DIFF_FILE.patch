diff --git a/README.md b/README.md
index 8690bc0..59b41d4 100644
--- a/README.md
+++ b/README.md
@@ -1,6 +1,6 @@
 # ICAN 数字人 Agent

-ICAN 是一个面向真实产品演进的数字人 Agent 底层骨架（当前版本 v0.1.37）。第一版先打通浏览器、认证网关、LangGraph 编排、MCP、RAG、数字人 Provider 和可观测性，不绑定具体行业人格或业务工具。
+ICAN 是一个面向真实产品演进的数字人 Agent 底层骨架（当前版本 v0.1.38）。第一版先打通浏览器、认证网关、LangGraph 编排、MCP、RAG、数字人 Provider 和可观测性，不绑定具体行业人格或业务工具。

 ## 目录

@@ -116,5 +116,5 @@ Set-Location ..\web
 npm run build
 ```

-本版本页面缓存与数字人运行时生命周期验收记录见 [`tmp-docs/digital-human-agent-v0.1.37-page-cache-task-book-已完成.md`](tmp-docs/digital-human-agent-v0.1.37-page-cache-task-book-已完成.md)，上一版用户体验锐评与评测图表验收记录见 [`tmp-docs/digital-human-agent-v0.1.36-ux-critique-chart-task-book-已完成.md`](tmp-docs/digital-human-agent-v0.1.36-ux-critique-chart-task-book-已完成.md)。
+本版本实时语音响应与音量反馈验收记录见 [`tmp-docs/digital-human-agent-v0.1.38-realtime-voice-task-book-已完成.md`](tmp-docs/digital-human-agent-v0.1.38-realtime-voice-task-book-已完成.md)，上一版页面缓存与数字人运行时生命周期验收记录见 [`tmp-docs/digital-human-agent-v0.1.37-page-cache-task-book-已完成.md`](tmp-docs/digital-human-agent-v0.1.37-page-cache-task-book-已完成.md)。
 控制台视觉与页面验收记录见 [`tmp-docs/digital-human-agent-v0.1.3-console-reference-task-book-已完成.md`](tmp-docs/digital-human-agent-v0.1.3-console-reference-task-book-已完成.md)。
diff --git a/apps/agent-service/app/__init__.py b/apps/agent-service/app/__init__.py
index 6e87239..c8bbae3 100644
--- a/apps/agent-service/app/__init__.py
+++ b/apps/agent-service/app/__init__.py
@@ -2,4 +2,4 @@

 __all__ = ["__version__"]

-__version__ = "0.1.37"
+__version__ = "0.1.38"
diff --git a/apps/agent-service/app/api/routes.py b/apps/agent-service/app/api/routes.py
index 60e5a8f..1be81cd 100644
--- a/apps/agent-service/app/api/routes.py
+++ b/apps/agent-service/app/api/routes.py
@@ -32,7 +32,7 @@ router = APIRouter()
 @router.get("/health", response_model=HealthResponse, include_in_schema=False)
 async def health(request: Request) -> HealthResponse:
     container = get_container(request)
-    return HealthResponse(service=container.settings.service_name, version="0.1.37")
+    return HealthResponse(service=container.settings.service_name, version="0.1.38")


 @router.get("/internal/providers", response_model=list[ProviderResponse])
diff --git a/apps/agent-service/app/main.py b/apps/agent-service/app/main.py
index 329d23e..45dfa9d 100644
--- a/apps/agent-service/app/main.py
+++ b/apps/agent-service/app/main.py
@@ -308,7 +308,7 @@ def create_app(
                 await app.state.container.response_cache.close()
             await app.state.container.observability.flush()

-    app = FastAPI(title="Digital Human Agent", version="0.1.37", lifespan=lifespan)
+    app = FastAPI(title="Digital Human Agent", version="0.1.38", lifespan=lifespan)
     app.state.container = service_container
     app.add_middleware(
         RequestBodyLimitMiddleware,
diff --git a/apps/agent-service/app/realtime/audio_handlers.py b/apps/agent-service/app/realtime/audio_handlers.py
index 8a6645a..d21ab3c 100644
--- a/apps/agent-service/app/realtime/audio_handlers.py
+++ b/apps/agent-service/app/realtime/audio_handlers.py
@@ -90,6 +90,21 @@ class RealtimeAudioHandlersMixin:
             "ack", request_id=message.request_id, action="audio_end", accepted=True,
             utterance_id=current[0] if current else None, revision=current[1] if current else None,
         )
+        if result.status == "final" and result.text and current:
+            # ASR is the input boundary for the same realtime run. Reuse the
+            # audio generation so the transcript cannot be stranded as a UI
+            # event without reaching the Agent graph.
+            await self._text(
+                RealtimeMessage(
+                    type="text",
+                    requestId=message.request_id,
+                    sessionId=self.state.session_id,
+                    utteranceId=current[0],
+                    revision=current[1],
+                    text=result.text,
+                    isFinal=True,
+                )
+            )

     async def _reset_ingress_safely(self) -> None:
         try:
diff --git a/apps/agent-service/pyproject.toml b/apps/agent-service/pyproject.toml
index b6d0ca6..2895eac 100644
--- a/apps/agent-service/pyproject.toml
+++ b/apps/agent-service/pyproject.toml
@@ -1,6 +1,6 @@
 [project]
 name = "digital-human-agent-service"
-version = "0.1.37"
+version = "0.1.38"
 description = "FastAPI and LangGraph runtime for the digital human agent"
 requires-python = ">=3.11"
 dependencies = [
diff --git a/apps/agent-service/tests/test_realtime_protocol.py b/apps/agent-service/tests/test_realtime_protocol.py
index ef4be75..08f8d4a 100644
--- a/apps/agent-service/tests/test_realtime_protocol.py
+++ b/apps/agent-service/tests/test_realtime_protocol.py
@@ -258,6 +258,32 @@ class _AudioHarness(RealtimeAudioHandlersMixin):
         self.events.append({"type": "audio_queue", "revision": stats.revision})


+class _FinalIngress:
+    async def finish(self):
+        from app.realtime.audio import TranscriptResult
+        return TranscriptResult(status="final", text="帮我查一下今天的天气", utterance_id="utt-final", revision=2)
+
+    async def reset(self):
+        return None
+
+
+class _FinalAudioHarness(RealtimeAudioHandlersMixin):
+    def __init__(self) -> None:
+        self.state = ConnectionState(session_id="s1", hello_received=True)
+        self.ingress = _FinalIngress()
+        self.events: list[dict[str, object]] = []
+        self.limits = type("Limits", (), {"max_audio_buffer_bytes": 1024})()
+
+    async def _emit(self, event_type: str, **fields):
+        self.events.append({"type": event_type, **fields})
+
+    async def _reset_ingress_safely(self):
+        return None
+
+    async def _text(self, message):
+        self.events.append({"type": "agent_text", "text": message.text, "utterance_id": message.utterance_id, "revision": message.revision})
+
+
 @pytest.mark.asyncio
 async def test_audio_start_failure_rolls_back_revision_for_retry() -> None:
     harness = _AudioHarness()
@@ -268,6 +294,19 @@ async def test_audio_start_failure_rolls_back_revision_for_retry() -> None:
     assert harness.events[-2]["accepted"] is True


+@pytest.mark.asyncio
+async def test_final_asr_transcript_is_forwarded_into_agent_text_run() -> None:
+    harness = _FinalAudioHarness()
+    await harness.state.start_audio("utt-final", 2)
+    await harness._audio_end(RealtimeMessage(type="audio_end", requestId="a2", utteranceId="utt-final", revision=2))
+    assert harness.events[-1] == {
+        "type": "agent_text",
+        "text": "帮我查一下今天的天气",
+        "utterance_id": "utt-final",
+        "revision": 2,
+    }
+
+
 class _TextCapacityHarness(RealtimeHandlersMixin):
     def __init__(self) -> None:
         self.state = ConnectionState(user_id="u1", user_name="Tester", session_id="s1", hello_received=True)
diff --git a/apps/auth-service/pom.xml b/apps/auth-service/pom.xml
index ef024ef..9cd02bf 100644
--- a/apps/auth-service/pom.xml
+++ b/apps/auth-service/pom.xml
@@ -13,7 +13,7 @@

     <groupId>com.ican.digitalhuman</groupId>
     <artifactId>auth-service</artifactId>
-    <version>0.1.37</version>
+    <version>0.1.38</version>
     <name>digital-human-auth-service</name>
     <description>Authentication gateway for the digital human agent</description>

diff --git a/apps/mcp-server/app/__init__.py b/apps/mcp-server/app/__init__.py
index f4f68b7..00f33df 100644
--- a/apps/mcp-server/app/__init__.py
+++ b/apps/mcp-server/app/__init__.py
@@ -1,3 +1,3 @@
 """MCP tool service."""

-__version__ = "0.1.37"
+__version__ = "0.1.38"
diff --git a/apps/mcp-server/app/server.py b/apps/mcp-server/app/server.py
index c808564..10ceb58 100644
--- a/apps/mcp-server/app/server.py
+++ b/apps/mcp-server/app/server.py
@@ -16,7 +16,7 @@ def create_server(settings: Settings | None = None) -> MCPServer:
     settings = settings or get_settings()
     server = MCPServer(
         name=settings.service_name,
-        version="0.1.37",
+        version="0.1.38",
         instructions="Deterministic chain-probe tools for the digital human agent.",
         debug=False,
     )
@@ -24,7 +24,7 @@ def create_server(settings: Settings | None = None) -> MCPServer:

     @server.custom_route("/health", methods=["GET"], include_in_schema=False)
     async def health(_: Request) -> JSONResponse:
-        return JSONResponse({"status": "ok", "service": settings.service_name, "version": "0.1.37"})
+        return JSONResponse({"status": "ok", "service": settings.service_name, "version": "0.1.38"})

     @server.custom_route("/ready", methods=["GET"], include_in_schema=False)
     async def ready(_: Request) -> JSONResponse:
diff --git a/apps/mcp-server/pyproject.toml b/apps/mcp-server/pyproject.toml
index 2581beb..a4c75d1 100644
--- a/apps/mcp-server/pyproject.toml
+++ b/apps/mcp-server/pyproject.toml
@@ -1,6 +1,6 @@
 [project]
 name = "digital-human-mcp-server"
-version = "0.1.37"
+version = "0.1.38"
 description = "Internal MCP 2.x Streamable HTTP probe server"
 requires-python = ">=3.11"
 dependencies = [
diff --git a/apps/web/package-lock.json b/apps/web/package-lock.json
index 3faaa82..3529afb 100644
--- a/apps/web/package-lock.json
+++ b/apps/web/package-lock.json
@@ -1,12 +1,12 @@
 {
   "name": "digital-human-agent-web",
-  "version": "0.1.37",
+  "version": "0.1.38",
   "lockfileVersion": 3,
   "requires": true,
   "packages": {
     "": {
       "name": "digital-human-agent-web",
-      "version": "0.1.37",
+      "version": "0.1.38",
       "dependencies": {
         "lucide-react": "^0.468.0",
         "react": "^18.3.1",
diff --git a/apps/web/package.json b/apps/web/package.json
index 12b66d7..4cbebe8 100644
--- a/apps/web/package.json
+++ b/apps/web/package.json
@@ -1,7 +1,7 @@
 {
   "name": "digital-human-agent-web",
   "private": true,
-  "version": "0.1.37",
+  "version": "0.1.38",
   "type": "module",
   "scripts": {
     "dev": "vite",
diff --git a/apps/web/src/features/chat/components/HomeConversationBar.tsx b/apps/web/src/features/chat/components/HomeConversationBar.tsx
index 7591193..2750ae7 100644
--- a/apps/web/src/features/chat/components/HomeConversationBar.tsx
+++ b/apps/web/src/features/chat/components/HomeConversationBar.tsx
@@ -1,5 +1,5 @@
 import { AudioLines, Mic, Send, Square } from 'lucide-react'
-import { useEffect, useRef, useState, type FormEvent, type KeyboardEvent } from 'react'
+import { useEffect, useRef, useState, type CSSProperties, type FormEvent, type KeyboardEvent } from 'react'
 import type { AvatarSession } from '../../../shared/api/types'
 import type { RealtimeController } from '../../realtime/model'
 import { browserSpeechSupported, createBrowserSpeechRecognition, readBrowserSpeechResults, type BrowserSpeechRecognition } from '../../realtime/browserSpeech'
@@ -16,6 +16,7 @@ interface HomeConversationBarProps {
 export function HomeConversationBar({ session, realtime, isSending, isCreating = false, onSend, onCreateSession }: HomeConversationBarProps) {
   const [draft, setDraft] = useState('')
   const [browserVoiceActive, setBrowserVoiceActive] = useState(false)
+  const [browserVoiceLevel, setBrowserVoiceLevel] = useState(0)
   const browserRecognitionRef = useRef<BrowserSpeechRecognition | null>(null)
   const browserVoiceStoppingRef = useRef(false)
   const browserRestartTimerRef = useRef<number | null>(null)
@@ -43,6 +44,55 @@ export function HomeConversationBar({ session, realtime, isSending, isCreating =
     browserRecognitionRef.current = null
   }, [])

+  useEffect(() => {
+    if (!browserVoiceActive || typeof navigator === 'undefined' || !navigator.mediaDevices?.getUserMedia) {
+      setBrowserVoiceLevel(0)
+      return
+    }
+    let active = true
+    let frame = 0
+    let stream: MediaStream | undefined
+    let context: AudioContext | undefined
+    let source: MediaStreamAudioSourceNode | undefined
+    let analyser: AnalyserNode | undefined
+    void navigator.mediaDevices.getUserMedia({ audio: true, video: false }).then(async (nextStream) => {
+      if (!active) {
+        nextStream.getTracks().forEach((track) => track.stop())
+        return
+      }
+      stream = nextStream
+      const Constructor = window.AudioContext ?? (window as Window & { webkitAudioContext?: typeof AudioContext }).webkitAudioContext
+      if (!Constructor) return
+      context = new Constructor()
+      await context.resume().catch(() => undefined)
+      source = context.createMediaStreamSource(nextStream)
+      analyser = context.createAnalyser()
+      analyser.fftSize = 512
+      source.connect(analyser)
+      const samples = new Uint8Array(analyser.fftSize)
+      const tick = () => {
+        if (!active || !analyser) return
+        analyser.getByteTimeDomainData(samples)
+        let energy = 0
+        for (const sample of samples) {
+          const centered = (sample - 128) / 128
+          energy += centered * centered
+        }
+        setBrowserVoiceLevel(Math.min(1, Math.sqrt(energy / samples.length) * 4))
+        frame = window.requestAnimationFrame(tick)
+      }
+      tick()
+    }).catch(() => undefined)
+    return () => {
+      active = false
+      window.cancelAnimationFrame(frame)
+      source?.disconnect()
+      stream?.getTracks().forEach((track) => track.stop())
+      void context?.close()
+      setBrowserVoiceLevel(0)
+    }
+  }, [browserVoiceActive])
+
   function submit(event: FormEvent) {
     event.preventDefault()
     const message = draft.trim()
@@ -65,8 +115,11 @@ export function HomeConversationBar({ session, realtime, isSending, isCreating =
   }

   async function toggleVoice() {
+    if (ready && realtime.state.audioSupported) {
+      await realtime.toggleRecording()
+      return
+    }
     if (!browserVoiceAvailable) {
-      if (ready && realtime.state.audioSupported) await realtime.toggleRecording()
       return
     }
     if (browserVoiceActive) {
@@ -136,6 +189,7 @@ export function HomeConversationBar({ session, realtime, isSending, isCreating =
     <form className="home-conversation-bar" onSubmit={submit} aria-label="数字人对话输入">
       <button
         className={'home-voice-button' + (recording || browserVoiceActive ? ' home-voice-button--active' : '')}
+        style={{ '--voice-level': String(Math.max(0, Math.min(1, recording ? realtime.state.audioLevel : browserVoiceLevel))) } as CSSProperties}
         type="button"
         onClick={() => void toggleVoice()}
         disabled={!voiceAvailable}
diff --git a/apps/web/src/features/realtime/components/RealtimePanel.tsx b/apps/web/src/features/realtime/components/RealtimePanel.tsx
index b85811d..7a0b9f9 100644
--- a/apps/web/src/features/realtime/components/RealtimePanel.tsx
+++ b/apps/web/src/features/realtime/components/RealtimePanel.tsx
@@ -1,4 +1,5 @@
 import { Mic, MicOff, Plug, Radio, RotateCcw, Square, Volume2, WifiOff } from 'lucide-react'
+import type { CSSProperties } from 'react'
 import { StatusPill } from '../../../shared/components/StatusPill'
 import type { RealtimeController } from '../model'

@@ -45,7 +46,7 @@ export function RealtimePanel({ realtime }: RealtimePanelProps) {
           </div>
           {state.audioSupported ? (
             <div className="realtime-controls">
-              <button className={`realtime-mic${recording ? ' realtime-mic--active' : ''}`} type="button" onClick={() => void realtime.toggleRecording()} disabled={connecting || state.connection !== 'connected'} aria-label={recording ? '结束录音' : '开始录音'} title={recording ? '结束录音' : '开始录音'}>
+              <button className={`realtime-mic${recording ? ' realtime-mic--active' : ''}`} style={{ '--voice-level': String(state.audioLevel) } as CSSProperties} type="button" onClick={() => void realtime.toggleRecording()} disabled={connecting || state.connection !== 'connected'} aria-label={recording ? '结束录音' : '开始录音'} title={recording ? '结束录音' : '开始录音'}>
                 {recording ? <Square size={16} /> : <Mic size={18} />}
                 <span>{recording ? '结束' : '说话'}</span>
               </button>
diff --git a/apps/web/src/features/realtime/pcm16.ts b/apps/web/src/features/realtime/pcm16.ts
index 138b007..f24c89a 100644
--- a/apps/web/src/features/realtime/pcm16.ts
+++ b/apps/web/src/features/realtime/pcm16.ts
@@ -145,6 +145,9 @@ export class Pcm16Recorder {
     }
     const resampled = this.resampler.push(downmixToMono(input, channels))
     if (!resampled.length) return
+    let energy = 0
+    for (const sample of resampled) energy += sample * sample
+    this.options.onLevel?.(Math.min(1, Math.sqrt(energy / resampled.length) * 4))
     this.appendPcm(floatToPcm16(resampled, this.channels))
   }

diff --git a/apps/web/src/features/realtime/runtime.ts b/apps/web/src/features/realtime/runtime.ts
index a54e27f..c5c8d6a 100644
--- a/apps/web/src/features/realtime/runtime.ts
+++ b/apps/web/src/features/realtime/runtime.ts
@@ -32,6 +32,9 @@ export class RealtimeRuntime {
   private audioStarted = false
   private operation = 0
   private disposed = false
+  private silenceTimer?: ReturnType<typeof setTimeout>
+  private maxRecordingTimer?: ReturnType<typeof setTimeout>
+  private speechDetected = false

   constructor(session: AvatarSession | null, dispatch: Dispatch, readOptions: OptionsReader) {
     this.sessionId = session?.sessionId
@@ -62,6 +65,8 @@ export class RealtimeRuntime {
     const recorder = this.recorder
     this.recorder = undefined
     if (recorder) await recorder.stop()
+    this.clearRecordingTimers()
+    this.dispatch({ type: 'audio_level', level: 0 })
     this.playback?.interrupt()
     if (this.playback) await this.playback.close()
     await this.transport?.dispose()
@@ -82,6 +87,8 @@ export class RealtimeRuntime {
     const previousRevision = this.revision
     this.recorder = undefined
     if (recorder) await recorder.stop()
+    this.clearRecordingTimers()
+    this.dispatch({ type: 'audio_level', level: 0 })
     if (this.audioStarted) this.sendAudioEnd(previousUtterance, previousRevision)
     this.playback?.interrupt()
     this.transport?.close()
@@ -115,6 +122,8 @@ export class RealtimeRuntime {
     this.utteranceOpen = true
     this.dispatch({ type: 'revision', utteranceId, revision })
     this.dispatch({ type: 'recording', state: 'requesting' })
+    this.speechDetected = false
+    this.clearRecordingTimers()
     const recorder = new Pcm16Recorder({
       sampleRate: config.sampleRate,
       channels: config.channels,
@@ -122,6 +131,7 @@ export class RealtimeRuntime {
       maxPendingBytes: config.maxAudioBufferBytes,
       onChunk: (frame) => this.sendAudioFrame(transport, config, operation, frame),
       onDrop: (count) => this.dispatch({ type: 'buffer', bytes: transport.bufferedAmount, dropped: count }),
+      onLevel: (level) => this.handleAudioLevel(level),
     })
     let audioStartSent = false
     try {
@@ -140,6 +150,7 @@ export class RealtimeRuntime {
       }
       this.recorder = recorder
       this.dispatch({ type: 'recording', state: 'recording' })
+      this.maxRecordingTimer = setTimeout(() => { void this.stopRecording() }, 15_000)
       return true
     } catch (cause) {
       await recorder.stop()
@@ -156,6 +167,8 @@ export class RealtimeRuntime {
     if (!recorder && !this.audioStarted) return
     this.dispatch({ type: 'recording', state: 'stopping' })
     if (recorder) await recorder.stop()
+    this.clearRecordingTimers()
+    this.dispatch({ type: 'audio_level', level: 0 })
     this.sendAudioEnd()
     this.utteranceOpen = false
     this.dispatch({ type: 'recording', state: 'idle' })
@@ -187,6 +200,8 @@ export class RealtimeRuntime {
     const previousRevision = this.revision
     this.recorder = undefined
     if (recorder) await recorder.stop()
+    this.clearRecordingTimers()
+    this.dispatch({ type: 'audio_level', level: 0 })
     if (this.audioStarted) this.sendAudioEnd(previousUtterance, previousRevision)
     this.utteranceOpen = false
     this.playback?.interrupt()
@@ -216,6 +231,29 @@ export class RealtimeRuntime {
     this.dispatch({ type: 'buffer', bytes: transport.bufferedAmount, dropped: sent ? 0 : 1 })
     return sent
   }
+
+  private handleAudioLevel(level: number): void {
+    const normalized = Math.max(0, Math.min(1, level))
+    this.dispatch({ type: 'audio_level', level: normalized })
+    if (normalized >= 0.035) {
+      this.speechDetected = true
+      if (this.silenceTimer) clearTimeout(this.silenceTimer)
+      this.silenceTimer = undefined
+      return
+    }
+    if (!this.speechDetected || this.silenceTimer) return
+    this.silenceTimer = setTimeout(() => {
+      this.silenceTimer = undefined
+      if (this.recorder?.isRecording) void this.stopRecording()
+    }, 650)
+  }
+
+  private clearRecordingTimers(): void {
+    if (this.silenceTimer) clearTimeout(this.silenceTimer)
+    if (this.maxRecordingTimer) clearTimeout(this.maxRecordingTimer)
+    this.silenceTimer = undefined
+    this.maxRecordingTimer = undefined
+  }
   private sendAudioEnd(utteranceId = this.utteranceId, revision = this.revision): void {
     const decision = decideAudioEnd(
       this.audioStarted && this.utteranceId ? { utteranceId: this.utteranceId, revision: this.revision } : undefined,
diff --git a/apps/web/src/features/realtime/state.ts b/apps/web/src/features/realtime/state.ts
index 9edf505..6eb74bd 100644
--- a/apps/web/src/features/realtime/state.ts
+++ b/apps/web/src/features/realtime/state.ts
@@ -100,6 +100,8 @@ export function realtimeReducer(state: RealtimeState, action: RealtimeAction): R
       }
     case 'buffer':
       return { ...state, bufferedBytes: Math.max(0, action.bytes), droppedFrames: state.droppedFrames + Math.max(0, action.dropped ?? 0), lastEventAt: new Date().toISOString() }
+    case 'audio_level':
+      return { ...state, audioLevel: Math.max(0, Math.min(1, action.level)), lastEventAt: new Date().toISOString() }
     case 'error':
       return { ...state, connection: action.fatal ? 'error' : state.connection, phase: 'error', statusText: sanitizeDisplayText(action.message, 180), error: sanitizeDisplayText(action.message, 180), lastEventAt: new Date().toISOString() }
     case 'event':
diff --git a/apps/web/src/features/realtime/types.ts b/apps/web/src/features/realtime/types.ts
index 3462878..8ec47be 100644
--- a/apps/web/src/features/realtime/types.ts
+++ b/apps/web/src/features/realtime/types.ts
@@ -50,6 +50,7 @@ export interface RealtimeState {
   error?: string
   droppedFrames: number
   bufferedBytes: number
+  audioLevel: number
   lastEventAt?: string
 }

@@ -70,6 +71,7 @@ export const initialRealtimeState: RealtimeState = {
   statusText: '当前仅支持文本实时链路',
   droppedFrames: 0,
   bufferedBytes: 0,
+  audioLevel: 0,
 }

 export type RealtimeAction =
@@ -84,6 +86,7 @@ export type RealtimeAction =
   | { type: 'transcript'; status: 'partial' | 'final' | 'unsupported'; text?: string }
   | { type: 'assistant'; text: string; append?: boolean }
   | { type: 'buffer'; bytes: number; dropped?: number }
+  | { type: 'audio_level'; level: number }
   | { type: 'error'; message: string; fatal?: boolean }
   | { type: 'event'; at?: string }

@@ -159,6 +162,7 @@ export interface PcmRecorderOptions {
   maxPendingBytes?: number
   onChunk: (frame: ArrayBuffer) => boolean | void
   onDrop?: (count: number) => void
+  onLevel?: (level: number) => void
 }

 export interface PcmPlaybackOptions {
@@ -173,6 +177,7 @@ export interface RealtimeSessionOptions {
   onTranscript?: (text: string) => void
   onAssistantText?: (text: string, append: boolean) => void
   onInterrupt?: () => void
+  onAudioLevel?: (level: number) => void
 }

 export function hasAudioInput(session?: { capabilities?: string[]; clientParams?: AvatarClientParams } | null): boolean {
diff --git a/apps/web/src/pages/home-reference.css b/apps/web/src/pages/home-reference.css
index e34b946..6a33edf 100644
--- a/apps/web/src/pages/home-reference.css
+++ b/apps/web/src/pages/home-reference.css
@@ -232,12 +232,35 @@
 }

 .home-voice-button {
+  --voice-level: 0;
+  position: relative;
+  isolation: isolate;
   width: 46px;
   height: 46px;
   color: #fff;
   background: var(--brand);
 }

+.home-voice-button::before,
+.home-voice-button::after {
+  position: absolute;
+  z-index: -1;
+  inset: calc(-5px - (var(--voice-level) * 10px));
+  border: 1px solid color-mix(in oklch, var(--danger) 48%, transparent);
+  border-radius: 50%;
+  opacity: calc(.16 + (var(--voice-level) * .52));
+  content: "";
+  transform: scale(calc(.72 + (var(--voice-level) * .34)));
+}
+.home-voice-button::after { inset: calc(-10px - (var(--voice-level) * 15px)); opacity: calc(.08 + (var(--voice-level) * .28)); transform: scale(calc(.68 + (var(--voice-level) * .28))); }
+.home-voice-button--active::before { animation: voice-ripple 1.1s ease-out infinite; }
+.home-voice-button--active::after { animation: voice-ripple 1.1s .35s ease-out infinite; }
+@keyframes voice-ripple { 0% { opacity: calc(.26 + (var(--voice-level) * .5)); } 70%, 100% { opacity: 0; transform: scale(calc(1.12 + (var(--voice-level) * .45))); } }
+@media (prefers-reduced-motion: reduce) {
+  .home-voice-button--active::before,
+  .home-voice-button--active::after { animation: none; }
+}
+
 .home-voice-button:hover:not(:disabled),
 .home-send-button:hover:not(:disabled) { background: var(--brand-strong); }
 .home-voice-button--active { background: var(--danger); }
diff --git a/apps/web/src/pages/pages.css b/apps/web/src/pages/pages.css
index ce0f3fb..4584767 100644
--- a/apps/web/src/pages/pages.css
+++ b/apps/web/src/pages/pages.css
@@ -175,7 +175,8 @@
 .chat-empty--compact { min-height: 160px; }.empty-icon { width: 46px; height: 46px; display: grid; place-items: center; margin-bottom: 11px; border-radius: 13px; color: var(--brand); background: var(--brand-soft); }.chat-empty h3 { margin: 0; color: var(--ink); font-size: 14px; }.chat-empty p { max-width: 310px; margin: 7px 0 0; color: var(--ink-muted); font-size: 11px; line-height: 1.6; }
 .composer { padding: 10px 13px 12px; border-top: 1px solid var(--line); background: var(--surface-muted); }.composer textarea { width: 100%; min-height: 52px; max-height: 140px; resize: vertical; padding: 9px 10px; border: 1px solid var(--line); border-radius: 7px; outline: 0; color: var(--ink); background: var(--surface); font-size: 12px; line-height: 1.5; }.composer textarea:focus { border-color: var(--brand); box-shadow: 0 0 0 3px var(--brand-soft); }.composer textarea::placeholder { color: var(--ink-faint); }.composer-footer { display: flex; align-items: center; justify-content: flex-end; gap: 7px; margin-top: 7px; color: var(--ink-faint); font-size: 10px; }.composer-footer > span { margin-right: auto; }.send-button, .stop-button { min-height: 31px; padding: 0 10px; font-size: 11px; }.send-button { color: var(--brand-ink); background: var(--brand); }.stop-button { color: oklch(0.46 0.16 24); border-color: color-mix(in oklch, var(--danger) 28%, var(--line)); background: var(--danger-soft); }
 .timeline-panel { min-height: 250px; padding: 16px 14px; }.timeline-heading { align-items: center; }.event-count, .panel-count { min-width: 23px; padding: 4px 6px; border-radius: 5px; color: var(--brand-strong); background: var(--brand-soft); text-align: center; font-size: 10px; font-weight: 750; }.timeline-empty { min-height: 180px; display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 9px; color: var(--ink-faint); text-align: center; }.timeline-empty p { max-width: 205px; margin: 0; font-size: 11px; line-height: 1.6; }.timeline-list { position: relative; display: grid; gap: 0; margin: 17px 0 0; padding: 0; list-style: none; }.timeline-list::before { position: absolute; top: 13px; bottom: 13px; left: 12px; width: 1px; background: var(--line); content: ""; }.timeline-item { position: relative; display: flex; gap: 9px; padding-bottom: 14px; }.timeline-item:last-child { padding-bottom: 0; }.timeline-icon { z-index: 1; width: 25px; height: 25px; flex: none; display: grid; place-items: center; border: 1px solid var(--line-strong); border-radius: 50%; color: var(--ink-muted); background: var(--surface); }.timeline-item--start .timeline-icon, .timeline-item--done .timeline-icon, .timeline-item--rag .timeline-icon, .timeline-item--filler .timeline-icon, .timeline-item--performance .timeline-icon { color: var(--brand); border-color: color-mix(in oklch, var(--brand) 35%, var(--line)); }.timeline-item--tool .timeline-icon, .timeline-item--disclosure .timeline-icon { color: #a66a1d; border-color: color-mix(in oklch, var(--warning) 35%, var(--line)); }.timeline-item--error .timeline-icon, .timeline-item--interrupted .timeline-icon { color: var(--danger); border-color: color-mix(in oklch, var(--danger) 35%, var(--line)); }.timeline-copy { min-width: 0; flex: 1; padding-top: 3px; }.timeline-copy > div { display: flex; gap: 6px; align-items: baseline; }.timeline-copy strong { min-width: 0; color: var(--ink); font-size: 10px; font-weight: 650; }.timeline-copy time { margin-left: auto; color: var(--ink-faint); font-size: 9px; white-space: nowrap; }.timeline-copy pre { max-height: 74px; overflow: auto; margin: 4px 0 0; color: var(--ink-muted); white-space: pre-wrap; overflow-wrap: anywhere; font: 10px/1.4 ui-monospace, SFMono-Regular, Consolas, monospace; }.mobile-timeline { display: none; }
-.realtime-panel { display: grid; gap: 9px; padding: 11px 13px; background: var(--surface); }.realtime-head, .realtime-actions, .realtime-status-line, .realtime-controls, .realtime-response > div, .realtime-degraded { display: flex; align-items: center; }.realtime-head { justify-content: space-between; gap: 10px; }.realtime-head .section-heading { margin: 0; }.realtime-head h2 { font-size: 14px; }.realtime-actions { gap: 6px; }.realtime-actions .status-pill { padding: 3px 6px; font-size: 9px; }.realtime-status-line { justify-content: space-between; gap: 8px; color: var(--ink-muted); font-size: 10px; }.realtime-status-line small { color: var(--ink-faint); font-size: 9px; }.realtime-transcript, .realtime-response { min-width: 0; padding: 8px 9px; border: 1px solid var(--line); border-radius: 7px; background: var(--surface-muted); }.realtime-transcript { display: grid; grid-template-columns: 29px minmax(0, 1fr); gap: 8px; }.realtime-transcript > span, .realtime-response > div span { color: var(--ink-faint); font-size: 9px; }.realtime-transcript p, .realtime-response p { min-width: 0; margin: 0; overflow: hidden; color: var(--ink); font-size: 11px; line-height: 1.5; text-overflow: ellipsis; white-space: nowrap; }.realtime-placeholder { color: var(--ink-faint) !important; }.realtime-response > div { gap: 5px; margin-bottom: 4px; }.realtime-response > div svg { color: var(--brand); }.realtime-controls { gap: 7px; }.realtime-mic { min-width: 68px; min-height: 31px; display: inline-flex; align-items: center; justify-content: center; gap: 6px; padding: 0 10px; border: 1px solid var(--brand); border-radius: 7px; color: var(--brand-ink); background: var(--brand); font-size: 11px; }.realtime-mic--active { color: #fff; border-color: var(--danger); background: var(--danger); }.realtime-stop { flex: none; }.realtime-controls small { min-width: 0; overflow: hidden; color: var(--ink-faint); font-size: 9px; text-overflow: ellipsis; white-space: nowrap; }.realtime-degraded { gap: 7px; min-height: 36px; padding: 0 9px; border: 1px solid color-mix(in oklch, var(--warning) 28%, var(--line)); border-radius: 7px; color: oklch(0.48 0.13 76); background: var(--warning-soft); font-size: 10px; }
+.realtime-panel { display: grid; gap: 9px; padding: 11px 13px; background: var(--surface); }.realtime-head, .realtime-actions, .realtime-status-line, .realtime-controls, .realtime-response > div, .realtime-degraded { display: flex; align-items: center; }.realtime-head { justify-content: space-between; gap: 10px; }.realtime-head .section-heading { margin: 0; }.realtime-head h2 { font-size: 14px; }.realtime-actions { gap: 6px; }.realtime-actions .status-pill { padding: 3px 6px; font-size: 9px; }.realtime-status-line { justify-content: space-between; gap: 8px; color: var(--ink-muted); font-size: 10px; }.realtime-status-line small { color: var(--ink-faint); font-size: 9px; }.realtime-transcript, .realtime-response { min-width: 0; padding: 8px 9px; border: 1px solid var(--line); border-radius: 7px; background: var(--surface-muted); }.realtime-transcript { display: grid; grid-template-columns: 29px minmax(0, 1fr); gap: 8px; }.realtime-transcript > span, .realtime-response > div span { color: var(--ink-faint); font-size: 9px; }.realtime-transcript p, .realtime-response p { min-width: 0; margin: 0; overflow: hidden; color: var(--ink); font-size: 11px; line-height: 1.5; text-overflow: ellipsis; white-space: nowrap; }.realtime-placeholder { color: var(--ink-faint) !important; }.realtime-response > div { gap: 5px; margin-bottom: 4px; }.realtime-response > div svg { color: var(--brand); }.realtime-controls { gap: 7px; }.realtime-mic { --voice-level: 0; position: relative; isolation: isolate; min-width: 68px; min-height: 31px; display: inline-flex; align-items: center; justify-content: center; gap: 6px; padding: 0 10px; border: 1px solid var(--brand); border-radius: 7px; color: var(--brand-ink); background: var(--brand); font-size: 11px; }.realtime-mic::before,.realtime-mic::after { position: absolute; z-index: -1; inset: calc(-4px - (var(--voice-level) * 8px)); border: 1px solid color-mix(in oklch, var(--danger) 48%, transparent); border-radius: 50%; opacity: calc(.15 + (var(--voice-level) * .5)); content: ""; }.realtime-mic::after { inset: calc(-8px - (var(--voice-level) * 12px)); opacity: calc(.07 + (var(--voice-level) * .25)); }.realtime-mic--active::before { animation: voice-ripple 1.1s ease-out infinite; }.realtime-mic--active::after { animation: voice-ripple 1.1s .35s ease-out infinite; }.realtime-mic--active { color: #fff; border-color: var(--danger); background: var(--danger); }.realtime-stop { flex: none; }.realtime-controls small { min-width: 0; overflow: hidden; color: var(--ink-faint); font-size: 9px; text-overflow: ellipsis; white-space: nowrap; }.realtime-degraded { gap: 7px; min-height: 36px; padding: 0 9px; border: 1px solid color-mix(in oklch, var(--warning) 28%, var(--line)); border-radius: 7px; color: oklch(0.48 0.13 76); background: var(--warning-soft); font-size: 10px; }
+@keyframes voice-ripple { 0% { opacity: .38; transform: scale(.78); } 70%, 100% { opacity: 0; transform: scale(1.18); } }

 @media (max-width: 1250px) {
   .console-main { padding-inline: 20px; }
diff --git a/apps/web/tests/realtime-lifecycle.test.mjs b/apps/web/tests/realtime-lifecycle.test.mjs
index eb96f2f..87d317b 100644
--- a/apps/web/tests/realtime-lifecycle.test.mjs
+++ b/apps/web/tests/realtime-lifecycle.test.mjs
@@ -30,3 +30,14 @@ test('continuous voice reads the latest session after asynchronous session creat
   assert.match(source, /if \(!sessionRef\.current\)/)
   assert.match(source, /onSendRef\.current\(finalText\)/)
 })
+
+test('microphone feedback exposes normalized level and silence auto-stop', async () => {
+  const pcm = await readFile(fileURLToPath(new URL('../src/features/realtime/pcm16.ts', import.meta.url)), 'utf8')
+  const runtime = await readFile(fileURLToPath(new URL('../src/features/realtime/runtime.ts', import.meta.url)), 'utf8')
+  const button = await readFile(fileURLToPath(new URL('../src/features/chat/components/HomeConversationBar.tsx', import.meta.url)), 'utf8')
+  assert.match(pcm, /onLevel\?\.\(Math\.min\(1, Math\.sqrt\(energy \/ resampled\.length\) \* 4\)\)/)
+  assert.match(runtime, /this\.silenceTimer = setTimeout\(/)
+  assert.match(runtime, /this\.silenceTimer = undefined/)
+  assert.match(runtime, /void this\.stopRecording\(\)/)
+  assert.match(button, /--voice-level/)
+})
diff --git a/docs/api-contract.md b/docs/api-contract.md
index 8f05c87..5f0e0ba 100644
--- a/docs/api-contract.md
+++ b/docs/api-contract.md
@@ -1,4 +1,4 @@
-# API 契约（v0.1.37）
+# API 契约（v0.1.38）

 ## 浏览器 API

diff --git a/docs/runbook.md b/docs/runbook.md
index db3e929..94115c0 100644
--- a/docs/runbook.md
+++ b/docs/runbook.md
@@ -25,6 +25,8 @@ docker compose up --build

 首页工作区采用 Keep-Alive 缓存：切换到 RAG、评测、审计或设置页时不卸载数字人组件，当前星云 SDK 会话保持连接；页面隐藏期间调用 `switchInvisibleMode()` 与 `changeAvatarVisible(false)` 降低渲染和推送开销，返回首页后恢复可见状态，不重新创建会话或 SDK。显式断开、退出登录和更换会话仍会执行完整销毁。

+实时语音在 ASR 返回 `final` transcript 后会自动进入 Agent run，不需要再次点击发送。浏览器端按 16 kHz PCM16 采集并计算归一化音量；检测到说话后连续静音约 650ms 自动发送 `audio_end`，最长录音 15 秒。麦克风按钮波纹半径随音量变化；若 ASR 未配置则回退到浏览器连续识别，识别权限或网络异常会保留可读状态并允许改用文本输入。
+
 默认 Mock Provider 只验证实时连接、PCM16 帧边界、心跳和中断生命周期。配置 `SESSION_STORE_BACKEND=redis` 后会话与 cleanup 队列进入 Redis；配置 `HTTP_ASR_ENDPOINT`、`HTTP_TTS_ENDPOINT` 后启用真实媒体适配，适配器使用复用连接、超时和响应体上限，故障时保留协议连接并报告降级状态。

 ### 会话资源参数
