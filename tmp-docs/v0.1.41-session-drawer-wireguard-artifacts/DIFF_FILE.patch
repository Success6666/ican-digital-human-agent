diff --git a/README.md b/README.md
index f9045c7..98c0aa3 100644
--- a/README.md
+++ b/README.md
@@ -1,6 +1,6 @@
 # ICAN 数字人 Agent

-ICAN 是一个面向真实产品演进的数字人 Agent 底层骨架（当前版本 v0.1.40）。第一版先打通浏览器、认证网关、LangGraph 编排、MCP、RAG、数字人 Provider 和可观测性，不绑定具体行业人格或业务工具。
+ICAN 是一个面向真实产品演进的数字人 Agent 底层骨架（当前版本 v0.1.41）。第一版先打通浏览器、认证网关、LangGraph 编排、MCP、RAG、数字人 Provider 和可观测性，不绑定具体行业人格或业务工具。

 ## 目录

@@ -116,5 +116,5 @@ Set-Location ..\web
 npm run build
 ```

-本版本实时语音链路验收记录见 [`tmp-docs/digital-human-agent-v0.1.40-realtime-chain-task-book-已完成.md`](tmp-docs/digital-human-agent-v0.1.40-realtime-chain-task-book-已完成.md)，上一版持续实时对话验收记录见 [`tmp-docs/digital-human-agent-v0.1.39-continuous-realtime-task-book-已完成.md`](tmp-docs/digital-human-agent-v0.1.39-continuous-realtime-task-book-已完成.md)。
+本版本会话记录与公网映射验收记录见 [`tmp-docs/digital-human-agent-v0.1.41-session-drawer-wireguard-task-book-已完成.md`](tmp-docs/digital-human-agent-v0.1.41-session-drawer-wireguard-task-book-已完成.md)，上一版实时语音链路验收记录见 [`tmp-docs/digital-human-agent-v0.1.40-realtime-chain-task-book-已完成.md`](tmp-docs/digital-human-agent-v0.1.40-realtime-chain-task-book-已完成.md)。
 控制台视觉与页面验收记录见 [`tmp-docs/digital-human-agent-v0.1.3-console-reference-task-book-已完成.md`](tmp-docs/digital-human-agent-v0.1.3-console-reference-task-book-已完成.md)。
diff --git a/apps/agent-service/app/__init__.py b/apps/agent-service/app/__init__.py
index 10e3774..999ab2b 100644
--- a/apps/agent-service/app/__init__.py
+++ b/apps/agent-service/app/__init__.py
@@ -2,4 +2,4 @@

 __all__ = ["__version__"]

-__version__ = "0.1.40"
+__version__ = "0.1.41"
diff --git a/apps/agent-service/app/agent/latency.py b/apps/agent-service/app/agent/latency.py
index a211bb5..70ca271 100644
--- a/apps/agent-service/app/agent/latency.py
+++ b/apps/agent-service/app/agent/latency.py
@@ -19,7 +19,9 @@ class AdaptiveFillerPolicy:
         IntentName.CHAT: ("我听到了，马上回应。", 80),
         IntentName.KNOWLEDGE: ("我先查一下相关资料。", 180),
         IntentName.TASK: ("我先确认执行步骤。", 160),
-        IntentName.UNKNOWN: ("我先理解一下你的意思。", 120),
+        # The stream already reaches the user quickly; do not insert a
+        # speculative sentence before intent classification.
+        IntentName.UNKNOWN: ("", 0),
         IntentName.CONTROL: ("好的，我先停下来。", 0),
     }

@@ -31,7 +33,7 @@ class AdaptiveFillerPolicy:
         text, expected_delay = self._copy[decision.name]
         phase = FillerPhase.ACKNOWLEDGE if decision.name == IntentName.CONTROL else FillerPhase.THINKING
         return FillerPlan(
-            should_emit=decision.name != IntentName.CONTROL,
+            should_emit=decision.name != IntentName.CONTROL and bool(text),
             text=text,
             phase=phase,
             expected_delay_ms=expected_delay,
diff --git a/apps/agent-service/app/api/routes.py b/apps/agent-service/app/api/routes.py
index 4e91eb2..0d10a89 100644
--- a/apps/agent-service/app/api/routes.py
+++ b/apps/agent-service/app/api/routes.py
@@ -32,7 +32,7 @@ router = APIRouter()
 @router.get("/health", response_model=HealthResponse, include_in_schema=False)
 async def health(request: Request) -> HealthResponse:
     container = get_container(request)
-    return HealthResponse(service=container.settings.service_name, version="0.1.40")
+    return HealthResponse(service=container.settings.service_name, version="0.1.41")


 @router.get("/internal/providers", response_model=list[ProviderResponse])
diff --git a/apps/agent-service/app/main.py b/apps/agent-service/app/main.py
index 4db0efe..9f52230 100644
--- a/apps/agent-service/app/main.py
+++ b/apps/agent-service/app/main.py
@@ -308,7 +308,7 @@ def create_app(
                 await app.state.container.response_cache.close()
             await app.state.container.observability.flush()

-    app = FastAPI(title="Digital Human Agent", version="0.1.40", lifespan=lifespan)
+    app = FastAPI(title="Digital Human Agent", version="0.1.41", lifespan=lifespan)
     app.state.container = service_container
     app.add_middleware(
         RequestBodyLimitMiddleware,
diff --git a/apps/agent-service/pyproject.toml b/apps/agent-service/pyproject.toml
index bca8854..8d8ba1c 100644
--- a/apps/agent-service/pyproject.toml
+++ b/apps/agent-service/pyproject.toml
@@ -1,6 +1,6 @@
 [project]
 name = "digital-human-agent-service"
-version = "0.1.40"
+version = "0.1.41"
 description = "FastAPI and LangGraph runtime for the digital human agent"
 requires-python = ">=3.11"
 dependencies = [
diff --git a/apps/agent-service/tests/test_agent_orchestration.py b/apps/agent-service/tests/test_agent_orchestration.py
index ae915fd..b311d25 100644
--- a/apps/agent-service/tests/test_agent_orchestration.py
+++ b/apps/agent-service/tests/test_agent_orchestration.py
@@ -83,6 +83,9 @@ def test_filler_and_micro_expression_are_semantic() -> None:
     assert plan.cue.gesture == "small_nod"
     assert plan.cue.lip_sync is False
     assert PerformancePlanner().for_phase(FillerPhase.SPEAKING).lip_sync is True
+    unknown = AdaptiveFillerPolicy().plan(IntentDecision(name=IntentName.UNKNOWN))
+    assert unknown.should_emit is False
+    assert unknown.text == ""


 def test_prompt_injection_assessment_is_conservative_and_refusal_is_safe() -> None:
@@ -144,7 +147,7 @@ async def test_interrupt_marks_run_before_provider_io() -> None:


 @pytest.mark.asyncio
-async def test_stream_emits_filler_performance_and_interrupt_event() -> None:
+async def test_stream_skips_speculative_filler_and_reports_interrupt_event() -> None:
     settings = Settings(
         internal_token="test-token",
         mcp_allow_local_fallback=False,
@@ -167,11 +170,9 @@ async def test_stream_emits_filler_performance_and_interrupt_event() -> None:
     )
     first = await anext(stream)
     assert first["event"] == "start"
-    filler = await anext(stream)
-    assert filler["event"] == "filler"
-    assert filler["data"]["performance"]["expression"] == "thinking"
     await container.store.mark_interrupted(session.session_id, run_id=run_id)
     remaining = [event async for event in stream]
+    assert all(event["event"] != "filler" for event in remaining)
     assert any(event["event"] == "interrupted" for event in remaining)
     done = [event for event in remaining if event["event"] == "done"]
     assert done and done[-1]["data"]["interrupted"] is True
diff --git a/apps/agent-service/tests/test_realtime_steering.py b/apps/agent-service/tests/test_realtime_steering.py
index daaa1dd..3a19b52 100644
--- a/apps/agent-service/tests/test_realtime_steering.py
+++ b/apps/agent-service/tests/test_realtime_steering.py
@@ -517,7 +517,6 @@ async def test_superseding_run_cancels_slow_classifier() -> None:
         user_id="u1", user_name="Tester", session_id="s-classifier", message="今天天气怎么样", run_id=first
     )
     assert (await stream.__anext__())["event"] == "start"
-    assert (await stream.__anext__())["event"] == "filler"
     interrupted_event = asyncio.create_task(stream.__anext__())
     await asyncio.wait_for(classifier.started.wait(), timeout=1)

diff --git a/apps/agent-service/tests/test_realtime_streaming.py b/apps/agent-service/tests/test_realtime_streaming.py
index 34bf1b1..1c8d75b 100644
--- a/apps/agent-service/tests/test_realtime_streaming.py
+++ b/apps/agent-service/tests/test_realtime_streaming.py
@@ -38,11 +38,13 @@ async def test_stream_emits_ack_before_slow_intent_classification() -> None:
         user_id="u1", user_name="Tester", session_id="s-first-byte", message="今天天气怎么样", run_id=run_id
     )
     start = await asyncio.wait_for(anext(stream), timeout=0.2)
-    filler = await asyncio.wait_for(anext(stream), timeout=0.2)
     assert start["event"] == "start"
-    assert filler["event"] == "filler"
-    assert filler["data"]["runId"] == run_id
-    assert classifier.started.is_set() is False
+    pending = asyncio.create_task(anext(stream))
+    await asyncio.wait_for(classifier.started.wait(), timeout=0.2)
+    assert pending.done() is False
+    pending.cancel()
+    with pytest.raises(asyncio.CancelledError):
+        await pending
     await stream.aclose()


@@ -203,7 +205,6 @@ async def test_stream_interrupt_reports_cancellation_latency() -> None:
         run_id=run_id,
     )
     assert (await anext(stream))["event"] == "start"
-    assert (await anext(stream))["event"] == "filler"
     pending = asyncio.create_task(anext(stream))
     await classifier.started.wait()
     await store.mark_interrupted("s-cancel-latency", run_id)
diff --git a/apps/auth-service/pom.xml b/apps/auth-service/pom.xml
index 5039e0e..63ee432 100644
--- a/apps/auth-service/pom.xml
+++ b/apps/auth-service/pom.xml
@@ -13,7 +13,7 @@

     <groupId>com.ican.digitalhuman</groupId>
     <artifactId>auth-service</artifactId>
-    <version>0.1.40</version>
+    <version>0.1.41</version>
     <name>digital-human-auth-service</name>
     <description>Authentication gateway for the digital human agent</description>

diff --git a/apps/mcp-server/app/__init__.py b/apps/mcp-server/app/__init__.py
index 377b7ed..00ed240 100644
--- a/apps/mcp-server/app/__init__.py
+++ b/apps/mcp-server/app/__init__.py
@@ -1,3 +1,3 @@
 """MCP tool service."""

-__version__ = "0.1.40"
+__version__ = "0.1.41"
diff --git a/apps/mcp-server/app/server.py b/apps/mcp-server/app/server.py
index 276a541..a2bbc49 100644
--- a/apps/mcp-server/app/server.py
+++ b/apps/mcp-server/app/server.py
@@ -16,7 +16,7 @@ def create_server(settings: Settings | None = None) -> MCPServer:
     settings = settings or get_settings()
     server = MCPServer(
         name=settings.service_name,
-        version="0.1.40",
+        version="0.1.41",
         instructions="Deterministic chain-probe tools for the digital human agent.",
         debug=False,
     )
@@ -24,7 +24,7 @@ def create_server(settings: Settings | None = None) -> MCPServer:

     @server.custom_route("/health", methods=["GET"], include_in_schema=False)
     async def health(_: Request) -> JSONResponse:
-        return JSONResponse({"status": "ok", "service": settings.service_name, "version": "0.1.40"})
+        return JSONResponse({"status": "ok", "service": settings.service_name, "version": "0.1.41"})

     @server.custom_route("/ready", methods=["GET"], include_in_schema=False)
     async def ready(_: Request) -> JSONResponse:
diff --git a/apps/mcp-server/pyproject.toml b/apps/mcp-server/pyproject.toml
index 713749e..9ba0bf8 100644
--- a/apps/mcp-server/pyproject.toml
+++ b/apps/mcp-server/pyproject.toml
@@ -1,6 +1,6 @@
 [project]
 name = "digital-human-mcp-server"
-version = "0.1.40"
+version = "0.1.41"
 description = "Internal MCP 2.x Streamable HTTP probe server"
 requires-python = ">=3.11"
 dependencies = [
diff --git a/apps/web/package-lock.json b/apps/web/package-lock.json
index ea7758d..3238cdd 100644
--- a/apps/web/package-lock.json
+++ b/apps/web/package-lock.json
@@ -1,12 +1,12 @@
 {
   "name": "digital-human-agent-web",
-  "version": "0.1.40",
+  "version": "0.1.41",
   "lockfileVersion": 3,
   "requires": true,
   "packages": {
     "": {
       "name": "digital-human-agent-web",
-      "version": "0.1.40",
+      "version": "0.1.41",
       "dependencies": {
         "lucide-react": "^0.468.0",
         "react": "^18.3.1",
diff --git a/apps/web/package.json b/apps/web/package.json
index 070434f..47e9730 100644
--- a/apps/web/package.json
+++ b/apps/web/package.json
@@ -1,7 +1,7 @@
 {
   "name": "digital-human-agent-web",
   "private": true,
-  "version": "0.1.40",
+  "version": "0.1.41",
   "type": "module",
   "scripts": {
     "dev": "vite",
diff --git a/apps/web/src/features/chat/components/ConversationDrawer.tsx b/apps/web/src/features/chat/components/ConversationDrawer.tsx
index 679eb11..39efac8 100644
--- a/apps/web/src/features/chat/components/ConversationDrawer.tsx
+++ b/apps/web/src/features/chat/components/ConversationDrawer.tsx
@@ -1,3 +1,4 @@
+import { useDeferredValue } from 'react'
 import { History, Trash2, X } from 'lucide-react'
 import type { ChatMessage } from '../model'
 import { MessageBubble } from './MessageBubble'
@@ -10,6 +11,7 @@ interface ConversationDrawerProps {
 }

 export function ConversationDrawer({ open, messages, onClose, onClear }: ConversationDrawerProps) {
+  const deferredMessages = useDeferredValue(messages)
   if (!open) return null

   return (
@@ -22,7 +24,7 @@ export function ConversationDrawer({ open, messages, onClose, onClear }: Convers
             <h2>对话记录</h2>
           </div>
           <div className="conversation-drawer-actions">
-            <button className="icon-button" type="button" title="清空对话记录" aria-label="清空对话记录" onClick={onClear} disabled={!messages.length}>
+            <button className="icon-button" type="button" title="清空对话记录" aria-label="清空对话记录" onClick={onClear} disabled={!deferredMessages.length}>
               <Trash2 size={16} />
             </button>
             <button className="icon-button" type="button" title="关闭对话记录" aria-label="关闭对话记录" onClick={onClose}>
@@ -31,7 +33,7 @@ export function ConversationDrawer({ open, messages, onClose, onClear }: Convers
           </div>
         </header>
         <div className="conversation-drawer-list">
-          {messages.length ? messages.map((message) => <MessageBubble key={message.id} message={message} />) : (
+          {deferredMessages.length ? deferredMessages.map((message) => <MessageBubble key={message.id} message={message} />) : (
             <div className="conversation-drawer-empty">暂无对话记录</div>
           )}
         </div>
diff --git a/apps/web/src/features/chat/components/MessageBubble.tsx b/apps/web/src/features/chat/components/MessageBubble.tsx
index d0b4427..5a4840b 100644
--- a/apps/web/src/features/chat/components/MessageBubble.tsx
+++ b/apps/web/src/features/chat/components/MessageBubble.tsx
@@ -1,8 +1,9 @@
+import { memo } from 'react'
 import { Bot, UserRound } from 'lucide-react'
 import type { ChatMessage } from '../model'
 import { formatTime } from '../../../shared/lib/format'

-export function MessageBubble({ message }: { message: ChatMessage }) {
+export const MessageBubble = memo(function MessageBubble({ message }: { message: ChatMessage }) {
   const isUser = message.role === 'user'
   return (
     <article className={`message-row ${isUser ? 'message-row--user' : 'message-row--assistant'}`}>
@@ -13,4 +14,13 @@ export function MessageBubble({ message }: { message: ChatMessage }) {
       </div>
     </article>
   )
-}
+}, (previous, next) => {
+  const before = previous.message
+  const after = next.message
+  return before.id === after.id
+    && before.role === after.role
+    && before.content === after.content
+    && before.statusText === after.statusText
+    && before.pending === after.pending
+    && before.createdAt === after.createdAt
+})
diff --git a/apps/web/src/pages/home-reference.css b/apps/web/src/pages/home-reference.css
index 6a33edf..22cb030 100644
--- a/apps/web/src/pages/home-reference.css
+++ b/apps/web/src/pages/home-reference.css
@@ -351,7 +351,7 @@
 }

 .conversation-drawer-actions { display: flex; gap: 5px; }
-.conversation-drawer-list { min-height: 0; flex: 1; overflow: auto; padding-top: 16px; }
+.conversation-drawer-list { min-height: 0; flex: 1; overflow: auto; padding-top: 16px; contain: content; content-visibility: auto; }
 .conversation-drawer-empty { padding: 48px 10px; color: var(--ink-faint); text-align: center; font-size: 12px; }

 @media (max-width: 820px) {
diff --git a/apps/web/tests/chat-link-contract.test.mjs b/apps/web/tests/chat-link-contract.test.mjs
index a3d5a53..2821ace 100644
--- a/apps/web/tests/chat-link-contract.test.mjs
+++ b/apps/web/tests/chat-link-contract.test.mjs
@@ -20,8 +20,9 @@ test('session creation is guarded while the provider session is being created',
   assert.match(source, /disabled=\{!draft\.trim\(\) \|\| !interactionReady \|\| isCreating\}/)
 })

-test('waiting filler is routed into the avatar speech stream before the first answer delta', () => {
+test('assistant speech only uses real response text', () => {
   assert.equal(homeSource.includes('message.content.trim() || message.statusText?.trim()'), true)
   assert.equal(homeSource.includes("text: latestAssistant.content || latestAssistant.statusText || ''"), true)
   assert.equal(homeSource.includes('pending: latestAssistant.content ? latestAssistant.pending : false'), true)
+  assert.equal(homeSource.includes('我先理解一下你的意思'), false)
 })
diff --git a/apps/web/tests/layout-contract.test.mjs b/apps/web/tests/layout-contract.test.mjs
index 4f3df64..df67df1 100644
--- a/apps/web/tests/layout-contract.test.mjs
+++ b/apps/web/tests/layout-contract.test.mjs
@@ -10,6 +10,8 @@ const homeCss = fs.readFileSync(path.resolve(here, '../src/pages/home-reference.
 const app = fs.readFileSync(path.resolve(here, '../src/app/AuthenticatedApp.tsx'), 'utf8')
 const runtimeSurface = fs.readFileSync(path.resolve(here, '../src/features/avatar/runtime/AvatarRuntimeSurface.tsx'), 'utf8')
 const mofaRuntime = fs.readFileSync(path.resolve(here, '../src/features/avatar/runtime/mofaRuntime.ts'), 'utf8')
+const drawer = fs.readFileSync(path.resolve(here, '../src/features/chat/components/ConversationDrawer.tsx'), 'utf8')
+const messageBubble = fs.readFileSync(path.resolve(here, '../src/features/chat/components/MessageBubble.tsx'), 'utf8')

 test('console keeps navigation fixed while main content owns scrolling', () => {
   assert.match(css, /\.console-shell\s*\{[^}]*overflow:\s*hidden/)
@@ -47,3 +49,10 @@ test('avatar runtime uses invisible mode instead of rebuilding the SDK', () => {
   assert.match(mofaRuntime, /changeAvatarVisible\(visible: boolean\)/)
   assert.match(mofaRuntime, /current\.destroy\('component_unmounted'\)/)
 })
+
+test('conversation drawer defers stream updates and memoizes stable messages', () => {
+  assert.match(drawer, /useDeferredValue\(messages\)/)
+  assert.match(drawer, /deferredMessages\.map\(/)
+  assert.match(messageBubble, /memo\(function MessageBubble/)
+  assert.match(homeCss, /\.conversation-drawer-list\s*\{[^}]*contain:\s*content[^}]*content-visibility:\s*auto/)
+})
diff --git a/docker-compose.yml b/docker-compose.yml
index 7e49350..fbffcdb 100644
--- a/docker-compose.yml
+++ b/docker-compose.yml
@@ -6,6 +6,7 @@ services:
       context: ./apps/web
     ports:
       - "127.0.0.1:${WEB_PORT:-8088}:80"
+      - "${WEB_WG_BIND_ADDRESS:-127.0.0.1}:${WEB_WG_PORT:-18088}:80"
     depends_on:
       auth-service:
         condition: service_healthy
diff --git a/docs/api-contract.md b/docs/api-contract.md
index 2ca9ff5..c1b596f 100644
--- a/docs/api-contract.md
+++ b/docs/api-contract.md
@@ -1,4 +1,4 @@
-# API 契约（v0.1.40）
+# API 契约（v0.1.41）

 ## 浏览器 API